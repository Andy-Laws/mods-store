# Database (MySQL). Use mysql:// for MySQL or MariaDB. Default port 3306.
# Example: mysql://USER:PASSWORD@HOST:3306/DATABASE
DATABASE_URL="mysql://u10_SXBbZ49HsE:1K4qaqh9l+xExJr.o=2d6.aw@192.34.101.43:3306/s10_subsqlwebsite"

# Auth.js - generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret"

# NextAuth base URL (Discord OAuth callback). Use https://yourdomain.com in production.
NEXTAUTH_URL="http://localhost:3000"

# Discord OAuth - create at https://discord.com/developers/applications
DISCORD_CLIENT_ID="1474907589702582363"
DISCORD_CLIENT_SECRET="Q_vBBv1sdWbCZn5mnd-RSpEqh2yBl-M2"

# Required for automatic role-based downloads: Discord server (guild) ID.
# Users' roles from this server are synced on sign-in and used for product access.
# Right-click your server in Discord → Copy Server ID (enable Developer Mode in Discord settings).
DISCORD_GUILD_ID=""

# Optional: Discord Bot token. When set, roles are re-synced when users open the dashboard or download
# (so new roles appear without re-login). Also used for "Grant access" to assign roles in Discord.
# Create a bot in the same Discord app, add it to your server, enable Server Members Intent.
DISCORD_BOT_TOKEN=""

# Optional: Discord server invite for "Join to purchase" CTA
NEXT_PUBLIC_DISCORD_INVITE_URL="https://discord.gg/your-invite"

# S3-compatible storage (e.g. Cloudflare R2). When set, all uploads go to S3 instead of local disk.
# R2: create API token in Cloudflare Dashboard → R2 → Manage R2 API Tokens.
S3_ENDPOINT="https://<account_id>.r2.cloudflarestorage.com"
S3_REGION="auto"
S3_ACCESS_KEY_ID=""
S3_SECRET_ACCESS_KEY=""
S3_PUBLIC_BUCKET=""
S3_PUBLIC_URL=""
S3_PRIVATE_BUCKET=""
